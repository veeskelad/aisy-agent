{
  "name": "aisy-harness",
  "private": true,
  "version": "0.1.4",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:watch": "pnpm -r --parallel test:watch",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
